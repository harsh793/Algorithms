def binary_search(d1, k):
    low = 0
    high = len(d1) - 1
    while low <= high:
        mid = math.floor((high + low)/ 2)
        if key == d1[mid]:
            return mid
        elif key <= d1[mid]:
            high = mid -1
        else:
            low = mid + 1
    return -1
