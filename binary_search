def binary_search(data, key):
    low = 0
    high = len(data) - 1
    while low <= high:
        mid = math.floor((high + low)/ 2)
        if key == data[mid]:
            return mid
        elif key <= data[mid]:
            high = mid -1
        else:
            low = mid + 1
    return -1
